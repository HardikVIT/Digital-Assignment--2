#include <stdio.h>
int power(int x,int n);
int main(){
    int n,x;
    scanf("%d",&x);
    scanf("%d",&n);
    printf("%d",power(x,n));
}
int power(int x,int n){
    if(n==0){
        return 1;
    }
    n--;
    return (x*power(x,n));
}
